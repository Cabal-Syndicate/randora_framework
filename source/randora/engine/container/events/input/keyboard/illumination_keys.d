module randora.engine.container.events.input.keyboard.illumination_keys;

mixin template IlluminationKeys(){
	/+
	import randora.engine.container.events.input.keyboard.key_event;
	mixin(KeyEvent!("kbdillumdown"));
	mixin(KeyEvent!("kbdillumtoggle"));
	mixin(KeyEvent!("kbdillumup"));
	+/
}
